build:
  docker:
    web: frontend/Dockerfile
    api: backend/Dockerfile
run:
  web: bash -c 'yarn build && yarn serve --port $PORT'
  api: bash -c "python manage.py makemigrations && python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
